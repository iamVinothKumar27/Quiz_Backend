# Copy this file to .env and fill values (DO NOT commit .env)

MONGODB_URI="YOUR_MONGODB_URI"
GOOGLE_CLIENT_ID="YOUR_GOOGLE_CLIENT_ID"
GOOGLE_CLIENT_SECRET="YOUR_GOOGLE_CLIENT_SECRET"
SESSION_SECRET="YOUR_SESSION_SECRET"

# ---------- Hosting-friendly URLs ----------
# Frontend URL used for OAuth redirect after login
FRONTEND_URL="http://localhost:5173"

# Public URL of this backend (Render/Railway/etc). Used to construct OAuth callback.
SERVER_URL="http://localhost:3001"

# Optional: explicitly set Google callback URL (must match Google Console)
# GOOGLE_CALLBACK_URL="http://localhost:3001/api/auth/google/callback"

# Extra allowed CORS origins (comma-separated) for hosted frontend(s)
# Example: CORS_ORIGINS="https://your-frontend.pages.dev,https://your-frontend.vercel.app"
CORS_ORIGINS=""
